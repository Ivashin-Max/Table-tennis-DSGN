import React from 'react'
import styled from 'styled-components';

const SRegistrationForm = styled.form`
  
`

const SForm = (props: any) => {
  return (
    <SRegistrationForm {...props} />
  )
}

export default SForm